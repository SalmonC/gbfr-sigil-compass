# ADR-0003：MVP 不开放第三方插件 SDK

- 状态：Accepted
- 日期：2026-07-22

## 背景

运行时插件需要发现、加载上下文、权限、签名、版本兼容和崩溃诊断。当前只有 Save 与未来 CV 两个已知来源。

## 决策

Application 暴露内部端口，官方 adapter 作为普通项目随应用发布。MVP 不扫描插件目录，不通过反射加载第三方程序集。

## 后果

- 保留替换实现的源码边界，同时减少攻击面和兼容承诺。
- 社区不能在不重新编译应用的情况下安装新 adapter。
- 真实需求出现后，可优先评估外部进程协议，而非进程内插件。
