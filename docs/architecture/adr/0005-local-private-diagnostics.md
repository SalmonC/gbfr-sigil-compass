# ADR-0005：诊断默认留在本地并执行数据最小化

- 状态：Accepted
- 日期：2026-07-22

## 背景

存档可能包含 Steam ID、玩家名和其他与配装无关的信息。完整存档对一般错误诊断没有必要。

## 决策

日志和 trace 仅写本地，不默认上传。诊断包由用户主动导出，包含版本、错误码、未知 hash、计数、请求与结果签名；默认排除存档、玩家身份和完整路径。

## 后果

- 降低隐私泄露风险。
- 某些解析问题仍需用户额外提供样本；应用必须单独说明风险并再次确认。
